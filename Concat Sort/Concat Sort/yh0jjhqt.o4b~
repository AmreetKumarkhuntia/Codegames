#include<iostream>
#include<vector>
#include<unordered_map>

using namespace std;

void solution(void) {
	vector<int> ar;
	unordered_map<int, int> m;
	int n, t;


	cin >> n;

	for (int i = 0; i < n; i++) {
		cin >> t;
		ar.push_back(t);
		m[t] = i;
	}

}

int main(void) {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int t;
	cin >> t;
	while (t > 0) {
		solution();
		t--;
	}

	return 0;
}